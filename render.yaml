services:
  - type: web
    name: verichain
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    disk:
      name: verichain-ledger-data
      mountPath: /var/data
      sizeGB: 1
